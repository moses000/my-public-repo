apiVersion: v1
kind: Service
metadata:
  name: go-consumer
spec:
  selector:
    app: go-consumer
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer # Expose the service to external traffic
